# Beenode Test Configuration for Polar Regtest
#
# Copy to .env and adjust as needed:
#   cp .env.test .env
#
# Or source before running:
#   source .env.test && beenode init --app test

# Application name
BEENODE_APP=beetest

# Network: bitcoin|testnet|signet|regtest
BEENODE_NETWORK=regtest

# Auth mode: pin|none
BEENODE_AUTH_MODE=pin

# BIP39 mnemonic (12 or 24 words)
# This is the well-known test mnemonic - NEVER use with real funds
BEENODE_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"

# Electrum server URL (electrs)
# For Polar regtest, you need to run electrs:
#   docker run -p 50001:50001 -e DAEMON_RPC_ADDR=host.docker.internal:18443 getumbrel/electrs
# Then use:
#   BEENODE_ELECTRUM=tcp://127.0.0.1:50001
BEENODE_ELECTRUM=

# Data directory for wallet persistence
BEENODE_DATA_DIR=./data

# Nostr relays (comma-separated)
BEENODE_RELAYS=

# Bitcoin RPC (for direct bitcoind access - future use)
BITCOIN_RPC_URL=http://127.0.0.1:18443
BITCOIN_RPC_USER=polaruser
BITCOIN_RPC_PASS=polarpass
BITCOIN_RPC_WALLET=

# Polar container info
POLAR_BITCOIND=polar-n3-backend1
